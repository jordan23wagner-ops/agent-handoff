[project]
name = "agent-handoff"
version = "0.1.0"
description = "Seamless handoff middleware for AI agents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"